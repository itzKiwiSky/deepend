return function(type, x, y, w, h)
    return {
        type = type,
        x = x,
        y = y,
        w = w,
        h = h,

        addHitbox = function(self, world)
            --world:add
        end
    }
end
