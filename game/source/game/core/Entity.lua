return function()
    return {
        x = x,
        y = y,
        r = 0,
        scaleX = 1,
        scaleY = 1,
        originX = 0,
        originY = 0,
        isEditorMode = registers.isEditorMode,

        serialize = function() end,
    }
end
